name: Deploy HealthGridIQ

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show deployment information
        run: |
          echo "Deployment branch:"
          git branch --show-current
          echo "Server:"
          hostname
          echo "User:"
          whoami
