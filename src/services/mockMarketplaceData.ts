import type { EquipmentItem, QuotationRequest } from '../types/marketplace';

// Zero mock data rule enforced across HealthGrid IQ
export const mockEquipmentCatalog: EquipmentItem[] = [];
export const mockInitialQuotations: QuotationRequest[] = [];
